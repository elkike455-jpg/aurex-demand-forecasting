# Scratch Notebooks

Use this folder only for temporary or model-specific development notebooks.

Examples:

- debugging a `TCN` implementation
- testing an `Informer` input pipeline
- validating a new `TFT` feature schema

Rule:

- do not place final paper comparison results here
- keep the main comparison in `../27_deep_sequence_model_experiments.ipynb`
